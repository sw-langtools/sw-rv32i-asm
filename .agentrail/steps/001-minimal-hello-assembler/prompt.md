Create the minimal sw-rv32i-asm crate needed for a hello-world MVP. Add Cargo.toml and src/lib.rs with an assemble(source: &str) -> Result<Vec<u8>, AsmError> API that depends on sw-rv32i-isa. Support comments, blank lines, canonical registers and ABI aliases through sw-rv32i-isa, decimal and 0x hex literals, offset(base) memory operands, and only these mnemonics: addi rd, rs1, imm; sb rs2, offset(rs1); ebreak. Emit bytes through sw-rv32i-isa encode_word, not local bit packing. Add tests for parsing, invalid source, byte output, and a hello-world source fixture string. Run cargo fmt --all --check, cargo test, cargo clippy --all-targets --all-features -- -D warnings, and markdown checker before committing and completing.