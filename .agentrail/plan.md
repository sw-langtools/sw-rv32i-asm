Build a vertical hello-world MVP before completing the full assembler. First create a minimal assembler crate that consumes sw-rv32i-isa and supports only the source forms needed for hello world: comments, registers and ABI aliases, decimal/hex literals, offset(base) memory operands, addi, sb, and ebreak. Then add a hello-world source fixture. Full labels, directives, listings, branches, and complete mnemonic coverage stay queued for later incremental steps after the MVP runs on the emulator.