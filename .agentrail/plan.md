Integrate shared ISA profile validation into the assembler before expanding syntax or CLI behavior. Start with a focused step that adds an assembly API accepting IsaProfile, keeps assemble() defaulting to RV32I, validates parsed instructions through sw-rv32i-isa, and tests RV32E register rejection plus RV32I hello-world compatibility.